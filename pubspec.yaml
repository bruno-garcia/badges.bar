name: badges_bar
version: 0.1.6
description: SVG badge generator from Metrics for packages on pub.dev. Likes, Pub Points, Popularity, Last Updated, Package Version and much more.
homepage: https://github.com/bruno-garcia/badges.bar

environment:
  sdk: ">=2.6.0 <3.0.0"

dependencies:
  http: ^0.12.2
  sentry: ^4.0.5

dev_dependencies:
  pedantic: ^1.9.2
  test: ^1.15.4
  test_coverage: ^0.4.3
